language: java
jdk: oraclejdk8
install: true
services:
  - mysql
before_install:
  - sudo mysql -e 'CREATE DATABASE IF NOT EXISTS digitaljournal;'
  - sudo mysql -e 'SET PASSWORD FOR `root`@`localhost` = `asdf`;'
script:
  - cd DigitalJournal
  - mvn clean install; cobertura:cobertura

after_success:
    - bash <(curl -s https://codecov.io/bash)